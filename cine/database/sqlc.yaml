version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: ["schema.sql", "schema-people.sql"]
    gen:
      go:
        package: "sqlite"
        out: "sqlite"
        emit_prepared_queries: true
